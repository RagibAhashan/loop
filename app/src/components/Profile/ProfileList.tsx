import { IProfile } from '@core/Profile';
import React from 'react';

interface Props {
    profiles: IProfile[];
}
const ProfileList: React.FunctionComponent<Props> = (props) => {
    return <div> prof list </div>;
};

export default ProfileList;
